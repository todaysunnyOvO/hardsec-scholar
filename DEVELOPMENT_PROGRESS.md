# HardSec Scholar 开发进度

> 本文档记录阶段状态、实际结果、遇到的问题、解决措施和方案调整。每个阶段完成并获得确认后，才进入下一阶段。

## 总体进度

| 阶段 | 内容 | 状态 | 开始日期 | 完成日期 |
| --- | --- | --- | --- | --- |
| M0 | 底座接入与环境核验 | 已完成 | 2026-08-20 | 2026-08-20 |
| M1 | 独立模块与工程质量基线 | 已完成 | 2026-08-20 | 2026-08-20 |
| M2 | 论文解析、切分与索引 | 已完成 | 2026-08-20 | 2026-08-20 |
| M3 | 混合检索与重排 | 已完成 | 2026-08-20 | 2026-08-20 |
| M4 | 基础 RAG、引用与拒答 | 已完成 | 2026-08-20 | 2026-08-20 |
| M5 | Agentic 工作流 | 已完成 | 2026-08-20 | 2026-08-20 |
| M6 | API、UI 与执行轨迹 | 已完成 | 2026-08-20 | 2026-08-20 |
| M7 | 评估、对照实验与交付材料 | 已完成 | 2026-08-21 | 2026-08-21 |
| M8 | 可控对话历史与历史管理 | 已完成 | 2026-08-21 | 2026-08-21 |

状态取值：`未开始`、`进行中`、`已完成`、`受阻`。

## M0：底座接入与环境核验

### 目标

- 将 Open Deep Research 接入当前工作区。
- 保护已经完成的 HardSec Scholar 开发文档。
- 核验 Git、Python、Node.js、uv 和上游依赖要求。
- 安装依赖并运行可离线执行的基础检查。
- 记录上游结构以及与原开发方案的偏差。

### 当前结果

| 检查项 | 结果 |
| --- | --- |
| Git | 2.52.0，可用 |
| Python | 3.10.8，可用 |
| Node.js | 24.10.0，可用 |
| uv | 0.12.5，已安装 |
| 初始工作区 | 仅包含 `DEVELOPMENT.md`，无 Git 仓库 |
| 上游仓库 | 已接入 `langchain-ai/open_deep_research` 的 `main` 分支 |
| 上游版本 | `0.0.16` |
| 上游提交 | `1b7d2e8` |
| 上游许可证 | MIT |
| 本地开发文档 | 已保留，当前为未跟踪文件 |
| 虚拟环境 | `.venv` 已创建，208 个包已安装 |
| 核心导入 | 通过 |
| Ruff | `src/open_deep_research` 全部通过 |
| 上游测试 | 仅收集到 1 个依赖真实模型的旧版质量测试 |

### 已完成操作

1. 初始化本地 Git 仓库。
2. 添加 Open Deep Research 为 `origin`。
3. 拉取并检出上游 `main` 分支。
4. 在检出前确认上游不存在同名 `DEVELOPMENT.md`，避免覆盖本地文档。
5. 核对上游 `pyproject.toml` 和源码目录。
6. 安装 uv 0.12.5，并通过上游 `uv.lock` 创建 `.venv`。
7. 完成核心图对象导入检查。
8. 对 `src/open_deep_research` 执行 Ruff，检查通过。
9. 执行 pytest 收集检查，确认上游现有测试范围。
10. 确认主扩展点为 `get_all_tools`、Researcher 子图和顶层 `deep_researcher` 图。

### 问题与解决措施

| 编号 | 问题 | 影响 | 解决措施 | 状态 |
| --- | --- | --- | --- | --- |
| M0-01 | 本地未安装 uv | 无法按上游锁文件创建一致环境 | 已通过当前 Python 安装 uv，并执行 `uv sync --extra dev` | 已解决 |
| M0-02 | 预先判断上游需要 Python 3.11+，但当前机器是 3.10.8 | 可能误导环境升级 | 已核验当前上游实际声明为 `requires-python = ">=3.10"`，因此保留 Python 3.10.8 | 已解决 |
| M0-03 | 当前目录已有开发文档，不能直接执行 `git clone ... .` | 可能导致克隆失败或文件冲突 | 使用 `git init + remote + fetch + checkout`，检出前检查同名文件 | 已解决 |
| M0-04 | 上游默认依赖多达 243 个解析包，实际安装 208 个包 | 安装较重，包含多个当前项目不会使用的供应商 SDK | M0 保留锁文件以保证上游可复现；M1 再决定如何隔离 HardSec Scholar 核心依赖，不在未验证前删除上游依赖 | 已记录 |
| M0-05 | `pyproject.toml` 支持 Python 3.10，但 `langgraph.json` 部署配置指定 3.11 | 本地开发与部署运行时版本不完全一致 | 本地导入和静态检查已在 3.10.8 通过；部署目标保留 3.11，M1 在 CI 配置中明确版本 | 已记录 |
| M0-06 | 上游只有一个可收集测试，且位于 legacy 质量评估中 | 不能作为可靠的离线回归基线 | HardSec Scholar 新增功能必须建立无 API Key 的单元测试和集成测试 | 已记录 |
| M0-07 | pytest 收集时出现 LangGraph/Pydantic 弃用警告 | 未来依赖大版本升级可能影响 legacy 代码 | 警告均来自 `src/legacy` 或第三方包；新代码不依赖 legacy，并使用当前 API | 已记录 |

### 方案动态调整

1. **Python 版本调整**：不主动升级到 Python 3.11。以上游当前 `pyproject.toml` 为准，Python 3.10.8 满足最低要求。若后续新增依赖明确要求更高版本，再单独评估升级。
2. **上游集成方式调整**：当前工作区直接跟踪 Open Deep Research 上游 `main`，HardSec Scholar 功能将放入独立包，避免大范围修改上游代码。
3. **测试策略调整**：不把上游 legacy 质量测试作为本项目基线。后续从零建立离线、可重复、无需模型密钥的测试集合。
4. **依赖策略调整**：当前阶段不精简上游依赖，以避免破坏底座；HardSec Scholar 自身新增依赖在 M1 中显式分组。

### 阶段验收结论

- 上游代码已经安全接入，原开发文档未被覆盖。
- 当前机器能够创建环境并导入核心 LangGraph 图。
- 上游核心代码通过 Ruff。
- 已识别工具注册、Researcher 子图和顶层图三个主要扩展点。
- 已明确依赖规模、Python 部署版本差异和上游测试不足等风险。
- M0 达到完成条件，可以在获得批准后进入 M1。

## M1：独立模块与工程质量基线

### 目标

- 建立与上游隔离的 `hardsec_scholar` Python 包。
- 建立非密钥配置、运行时密钥配置和默认配置文件。
- 建立硬件安全领域枚举、证据 Schema 和术语扩展机制。
- 建立无 API Key 的单元测试、Lint、类型检查和 CI 基线。
- 确认项目可以构建为 Python 发布包，且不携带 legacy 代码。

### 完成结果

| 项目 | 结果 |
| --- | --- |
| 发布包名称 | `hardsec-scholar` |
| 项目版本 | `0.1.0` |
| 独立源码包 | `src/hardsec_scholar` |
| 上游兼容包 | 保留 `src/open_deep_research` |
| 配置文件 | `config/default.yaml`、`config/terminology.yaml` |
| 领域模型 | ResearchArea、QuestionType、Paper、Chunk、Evidence、Citation 等 |
| 术语扩展 | 确定性 YAML 词典与查询变体生成 |
| 单元测试 | 9 个，全部通过 |
| Ruff 格式 | 通过 |
| Ruff 检查 | 通过 |
| mypy | 5 个源码文件全部通过 |
| 构建 | sdist 和 wheel 均构建成功 |
| legacy 打包检查 | sdist 0 条、wheel 0 条 |
| CI | Python 3.11 + uv + Ruff + mypy + pytest |

### 新增结构

```text
src/hardsec_scholar/
├── __init__.py
├── config.py
├── py.typed
└── domain/
    ├── __init__.py
    ├── models.py
    └── terminology.py

config/
├── default.yaml
└── terminology.yaml

tests/unit/
├── test_config.py
├── test_domain_models.py
└── test_terminology.py
```

### 问题与解决措施

| 编号 | 问题 | 影响 | 解决措施 | 状态 |
| --- | --- | --- | --- | --- |
| M1-01 | 首轮 Ruff 检查发现 4 个文件需要格式化、1 个导入顺序问题 | CI 会失败 | 使用 Ruff 统一格式并重新检查 | 已解决 |
| M1-02 | mypy 报告 PyYAML 缺少类型信息 | 类型检查无法通过 | 将 `types-PyYAML` 加入开发依赖 | 已解决 |
| M1-03 | 首次构建会把 `src/legacy` 及其测试加入发布包 | 发布包携带废弃代码和无关测试 | 收紧 setuptools 包范围，并用 `MANIFEST.in` 显式排除 legacy | 已解决 |
| M1-04 | 旧式 `project.license` TOML 表触发 setuptools 弃用警告 | 未来 setuptools 版本可能拒绝构建 | 改用 SPDX 字符串 `license = "MIT"` | 已解决 |
| M1-05 | 尝试清理本地构建缓存的命令被安全策略拦截 | 无法通过删除缓存验证干净构建 | 未删除任何文件；改用显式 Manifest 后非破坏性重建并检查压缩包内容 | 已解决 |
| M1-06 | 上游默认把 pytest 放在运行依赖中且供应商依赖很多 | 核心依赖仍然偏重 | 本阶段仅新增依赖分组，不贸然重构上游依赖；待功能稳定后再评估精简 | 已记录 |

### 方案动态调整

1. **类型检查工具**：开发文档原计划使用 Pyright。考虑到上游已经配置 mypy，本项目改用 Ruff + mypy，避免维护重复工具链。
2. **发布包名称**：将项目发行名从 `open_deep_research` 调整为 `hardsec-scholar`，但继续打包上游核心模块以保持兼容。
3. **Legacy 隔离**：源代码仍保留上游 `src/legacy` 便于追溯，但 HardSec Scholar 不导入、不测试、不发布该目录。
4. **密钥校验时机**：离线导入和测试不强制 API Key；只有创建实际模型客户端时才校验密钥，保证 CI 无密钥可运行。

### 阶段验收结论

- HardSec Scholar 已形成独立、类型化的应用包。
- 默认配置和运行时密钥配置已经分离。
- 硬件安全研究方向、问题类型、论文片段、证据和引用已建立统一 Schema。
- 术语扩展是确定性的，可在无模型环境中测试。
- 本地质量检查、单元测试和构建全部通过。
- M1 达到完成条件，可以在获得批准后进入论文解析、切分和索引阶段。

## M2：论文解析、切分与本地语料索引

### 目标

- 校验 PDF 文件类型、大小、可读性和文本层。
- 按物理页提取文本块、坐标和字号。
- 计算稳定内容哈希并检测重复论文。
- 启发式提取标题、作者、年份、DOI、摘要和研究方向。
- 识别常见论文章节并生成保留页码的 Chunk。
- 将论文元数据和 Chunk 事务性保存到 SQLite。
- 将原始 PDF 复制到受控的本地论文目录。

### 完成结果

| 项目 | 结果 |
| --- | --- |
| PDF 解析器 | PyMuPDF，按页保留文本块、坐标和字号 |
| 内容去重 | 流式 SHA-256，使用稳定论文 ID |
| 扫描件识别 | 可提取字符过少时返回 OCR 不支持错误 |
| 元数据 | 标题、作者、年份、DOI、摘要、研究方向 |
| 领域分类 | 侧信道、体系结构安全、硬件模糊测试、其他 |
| 章节识别 | Abstract、Introduction、Threat Model、Evaluation 等 |
| Chunk | tiktoken 计数、章节边界、物理页码、有限重叠 |
| References | 默认从可检索语料排除 |
| 本地语料索引 | SQLite 保存 Paper 与 PaperChunk，外键级联删除 |
| 摄取服务 | 解析、复制、切分和保存的一体化事务流程 |
| 测试 | 15 个测试全部通过 |
| Ruff | 28 个文件格式检查通过，Lint 通过 |
| mypy | 13 个源码文件通过 |

### 新增结构

```text
src/hardsec_scholar/
├── ingestion/
│   ├── __init__.py
│   ├── models.py
│   ├── parser.py
│   ├── metadata.py
│   ├── chunker.py
│   └── service.py
└── storage/
    ├── __init__.py
    └── repository.py

tests/
├── conftest.py
├── unit/
│   ├── test_pdf_parser.py
│   ├── test_metadata.py
│   ├── test_chunker.py
│   └── test_repository.py
└── integration/
    └── test_ingestion_service.py
```

### 问题与解决措施

| 编号 | 问题 | 影响 | 解决措施 | 状态 |
| --- | --- | --- | --- | --- |
| M2-01 | PDF 文本提取仅保留纯文本会丢失标题和章节字号信息 | 元数据与章节识别可靠性下降 | 使用 PyMuPDF dict 输出，同时保留文本块坐标和最大字号 | 已解决 |
| M2-02 | 固定字符切分无法稳定控制模型上下文 | Chunk 大小与配置不一致 | 显式加入 tiktoken，使用 token 窗口和有限重叠 | 已解决 |
| M2-03 | 首轮静态检查发现构造函数文档和 PDF bbox 类型问题 | CI 无法通过 | 补充公开构造函数文档，将 bbox 显式转换为四元组 | 已解决 |
| M2-04 | PyMuPDF 在 Python 退出时产生 SWIG 类型弃用警告 | 测试输出含 5 条第三方警告 | 确认警告来自 PyMuPDF 绑定且不影响测试；暂不全局屏蔽，便于后续依赖升级观察 | 已记录 |
| M2-05 | 真实论文版式差异很大，纯规则元数据提取不可能始终准确 | 某些论文标题、作者或章节可能识别错误 | Schema 允许字段后续人工修正；M2 使用确定性启发式，真实语料测试后再增强，不引入 LLM 解析依赖 | 已记录 |
| M2-06 | 首次格式化命令范围过宽，格式化了 6 个上游评估脚本 | 产生与本项目无关的上游文件变更 | 仅反向应用这 6 个文件的格式差异并恢复到 HEAD；后续质量命令明确限定新增目录 | 已解决 |

### 方案动态调整

1. **本地索引边界**：本阶段的“本地索引”指 SQLite 中的 Paper/Chunk 语料索引。向量和 BM25 排名属于下一阶段，避免解析与检索错误耦合。
2. **分词方式**：原方案只描述 Token 大小，实际选用 `cl100k_base` 进行稳定计数，并将 tiktoken 设为直接依赖。
3. **研究方向识别**：第一版使用透明的关键词计分，不调用 LLM。错误分类可人工修改，之后再根据真实 10 篇论文完善词表。
4. **状态拆分**：新增 `PaperStatus`，M2 成功结束时为 `parsed`；完成向量/关键词索引后才进入 `indexed`。
5. **测试 PDF**：测试期间动态生成两页英文硬件模糊测试 PDF，不提交任何用户论文或受版权保护内容。

### 阶段验收结论

- 可以离线完成文本型英文论文的校验、解析、元数据提取、章节切分和持久化。
- 每个 Chunk 都包含稳定 ID、论文 ID、章节、起止页码和正文。
- 重复 PDF 会在复制和再次保存之前被拒绝。
- 数据库写入失败时会清理本次创建的受控 PDF 副本，不影响用户源文件。
- 删除论文时，数据库中的 Chunk 会通过外键级联删除。
- M2 达到完成条件，可以在获得批准后进入混合检索与重排阶段。

## M3：混合检索与重排

### 目标

- 使用本地 Chroma 持久化论文 Chunk 的稠密向量。
- 使用适合小规模英文技术语料的 BM25 关键词检索。
- 通过 RRF 融合稠密检索和关键词检索结果。
- 提供可替换的 Embedding 与 Reranker 接口。
- 支持按论文范围过滤，并保留各阶段分数与排名以便解释。
- 建立无需 API Key、无需下载真实模型的确定性测试。

### 完成结果

| 项目 | 结果 |
| --- | --- |
| 稠密索引 | Chroma PersistentClient，本地持久化、余弦距离、自带 Embedding 关闭 |
| Embedding | OpenAI 适配器，通过协议支持测试替身和后续替换 |
| 关键词检索 | BM25Plus，英文技术标识符友好的确定性分词 |
| 排名融合 | Reciprocal Rank Fusion，默认 `k=60` |
| Reranker | FlashRank 适配器，可配置模型与缓存目录；支持 Identity 降级 |
| 检索过滤 | 稠密与 BM25 两路均支持限定论文 ID |
| 可解释分数 | Evidence 保存 dense、BM25、fusion、rerank 的排名或分数 |
| 索引状态 | 向量写入成功后将 Paper 从 `parsed` 更新为 `indexed` |
| 测试 | 24 个测试全部通过，其中包含端到端混合检索集成测试 |
| Ruff | 36 个项目文件格式检查通过，Lint 通过 |
| mypy | 22 个源码文件全部通过 |

### 新增结构

```text
src/hardsec_scholar/retrieval/
├── __init__.py
├── interfaces.py
├── embeddings.py
├── vector.py
├── bm25.py
├── fusion.py
├── reranker.py
├── hybrid.py
└── indexing.py

tests/unit/
├── test_embeddings.py
├── test_bm25.py
├── test_fusion.py
└── test_reranker.py

tests/integration/
└── test_hybrid_retrieval.py
```

### 问题与解决措施

| 编号 | 问题 | 影响 | 解决措施 | 状态 |
| --- | --- | --- | --- | --- |
| M3-01 | FlashRank 间接解析到 ONNXRuntime 1.24，而该版本没有 Python 3.10 Windows wheel | 当前环境无法安装依赖 | 对 Python 3.10 将 ONNXRuntime 约束为 `<1.24.0`，解析到兼容的 1.23.2 | 已解决 |
| M3-02 | Okapi BM25 在两文档语料中，查询词恰好出现于一篇时 IDF 为 0 | 极小论文集合可能把明确命中的结果过滤掉 | 改用 BM25Plus；它保留 BM25 的长度归一化，同时对小语料给出稳定正分 | 已解决 |
| M3-03 | mypy 1.16 在分析 LangSmith 的新类型定义时内部崩溃 | 无法完成类型检查，也不会报告本项目真实错误 | 将开发依赖下限提升到 1.17.1，本次锁定 1.20.2；升级后成功发现并修复 Chroma 类型边界 | 已解决 |
| M3-04 | Chroma 的 Embedding 参数使用协变 Sequence 联合类型，直接传 `list[list[float]]` 触发 mypy 不变性错误 | 静态检查失败 | 在 Chroma 适配器边界显式收窄为 `Sequence[float]`，不使用全局忽略 | 已解决 |
| M3-05 | 真实 OpenAI Embedding 和 FlashRank 模型会产生网络、密钥与下载依赖 | CI 不稳定，离线开发无法复现 | 接口支持依赖注入；测试使用确定性假 Embedding 和假 Ranker，真实客户端留到运行配置启用 | 已解决 |
| M3-06 | PyMuPDF 的 5 条 SWIG 弃用警告仍出现在完整测试中 | 测试输出有第三方警告 | 与 M2 一致，确认不影响检索测试且暂不屏蔽 | 已记录 |

### 方案动态调整

1. **BM25 变体**：从 Okapi BM25 调整为 BM25Plus。原因不是测试数据特例，而是目标语料本身只有约 10 篇论文，BM25Plus 对极小集合的 IDF 更稳健。
2. **关键词索引策略**：BM25 暂时在查询时从 SQLite Chunk 重建。以当前约 10 篇论文的规模，简单性和一致性优先于持久化；规模增长后再增加缓存或增量索引。
3. **模型边界**：核心检索器依赖 `EmbeddingProvider` 与 `Reranker` 协议，不直接绑定在线服务。OpenAI 和 FlashRank 只是首个实现，后续评估时可替换而不改融合逻辑。
4. **重排降级**：增加 Identity Reranker，使未下载模型时仍可使用 RRF 结果，而不是让整个问答链路不可用。
5. **可解释性**：Evidence 不只保存最终分数，还保留稠密、BM25 和融合阶段信息，便于后续 UI 展示 Agent 执行轨迹和面试演示。

### 阶段验收结论

- 论文 Chunk 可以写入本地 Chroma，并在成功后更新论文索引状态。
- 一个查询会同时经过稠密与 BM25Plus 检索，再由 RRF 融合并进入可替换重排器。
- 混合检索支持限定论文集合，返回证据保留论文、章节、页码和各阶段分数。
- 集成测试覆盖 SQLite 语料、Chroma 索引、两路召回、融合、过滤和删除流程。
- 24 项测试、Ruff 和 mypy 均通过，测试不需要 API Key 或真实模型下载。
- M3 达到完成条件，可以在获得批准后进入基础 RAG、引用与拒答阶段。

## M4：基础 RAG、引用与拒答

### 目标

- 将混合检索结果组装为有明确来源边界的受限上下文。
- 让生成模型只输出绑定 Evidence ID 的结构化结论。
- 将 Evidence ID 解析为论文、章节、页码和原文证据。
- 对无检索结果、模型判断证据不足和非法引用统一拒答。
- 建立无需真实模型或 API Key 的回答与拒答回归测试。

### 完成结果

| 项目 | 结果 |
| --- | --- |
| 上下文选择 | 按检索顺序去重，限制总证据数和每篇论文证据数 |
| 来源边界 | 每段证据独立呈现稳定 ID、论文、章节、页码和正文 |
| 模型输出 | Pydantic 结构化 `AnswerDraft`，每条 Claim 强制至少一个 Evidence ID |
| 生成约束 | 系统提示要求仅使用提供证据，并以问题原语言回答 |
| 答案渲染 | 仅从通过校验的 Claim 生成正文，行内展示 Evidence ID |
| 引用解析 | Citation 包含 Evidence ID、论文 ID、标题、章节、页码和对应 Claim |
| 引用完整性 | 拒绝未知 Evidence ID、同一 Claim 重复绑定和无有效引用答案 |
| 拒答 | 无召回、模型报告证据缺口或引用校验失败时统一 fail-closed |
| 可审计输出 | 返回所用原文证据、缺失证据、校验错误和限定论文范围 |
| 配置 | 上下文最多 6 条证据，每篇最多 3 条，拒答文本可配置 |
| 测试 | 36 个测试全部通过，包含 SQLite/Chroma 到引用答案的跨模块测试 |
| Ruff | 47 个项目文件格式和 Lint 检查通过 |
| mypy | 29 个源码文件全部通过 |

### 新增结构

```text
src/hardsec_scholar/generation/
├── __init__.py
├── models.py
├── interfaces.py
├── context.py
├── generator.py
├── verifier.py
└── service.py

tests/unit/
├── test_generation_context.py
├── test_answer_generator.py
└── test_citation_verifier.py

tests/integration/
└── test_basic_rag_service.py
```

### 问题与解决措施

| 编号 | 问题 | 影响 | 解决措施 | 状态 |
| --- | --- | --- | --- | --- |
| M4-01 | 自由文本回答无法可靠判断每个关键结论是否有引用 | 模型可能生成看似完整但无法追溯的段落 | 模型不直接返回最终长文本，改为输出原子 Claim 和强制非空的 Evidence ID 列表 | 已解决 |
| M4-02 | 模型可能输出不存在或未进入上下文的 Evidence ID | 产生伪造引用 | 只允许引用本轮最终选定证据；未知 ID 使整个回答校验失败并拒答 | 已解决 |
| M4-03 | 单篇论文的相邻结果可能占满整个上下文 | 比较问题或跨论文问题的证据多样性不足 | 上下文构建器同时限制总条数和每篇论文条数，并保留原检索次序 | 已解决 |
| M4-04 | 没有证据时仍调用生成模型 | 增加费用且可能诱发基于模型记忆作答 | 零召回时跳过模型，直接返回标准拒答和缺失证据说明 | 已解决 |
| M4-05 | 引用 ID 合法不等于引用在语义上支持 Claim | 仍需处理“真引用、错结论”的风险 | 本阶段校验引用完整性并在失败时拒答；语义支持审查与一次自动修复明确安排到 M5 Agentic 工作流 | 已记录 |
| M4-06 | 仅用独立测试替身无法证明 M3 与 M4 接口实际兼容 | 组合后可能出现类型或证据元数据丢失 | 新增真实 SQLite + Chroma + HybridRetriever 到 BasicRAGService 的跨模块集成测试 | 已解决 |
| M4-07 | PyMuPDF 的 5 条 SWIG 弃用警告继续出现在完整测试中 | 测试输出包含第三方警告 | 警告与生成链路无关且不影响结果，继续保留观察 | 已记录 |

### 方案动态调整

1. **生成数据结构**：不让模型直接生成包含任意 Markdown 引用的完整答案，改用 `ClaimDraft[]`。最终正文由系统确定性渲染，结构上保证每条结论都带 Evidence ID。
2. **拒答策略**：采用 fail-closed。无证据、模型主动报告证据不足、引用校验失败三种情况都不会返回部分可信的猜测答案。
3. **上下文控制**：在生成层再次去重并设置每篇论文上限，不依赖检索器一定返回理想分布，为后续比较类问题保留扩展空间。
4. **模型接入方式**：生成器接受任意 LangChain `BaseChatModel` 并使用 structured output，因此可以复用 Open Deep Research 的模型底座；测试使用注入替身，不调用在线模型。
5. **验证职责拆分**：M4 完成确定性的引用完整性验证；需要额外模型判断的语义支持验证、修复循环和重试上限放在 M5，避免把基础 RAG 与 Agent 状态机耦合。

### 阶段验收结论

- 单论文事实检索结果可以生成带稳定 Evidence ID 的回答。
- 引用可以回溯到论文标题、章节、物理页码和完整证据片段。
- 最终答案只能由带至少一个证据 ID 的结构化 Claim 组成。
- 文档无结果、模型声明缺少证据或引用 ID 非法时，系统返回明确拒答而不是猜测。
- 真实 SQLite/Chroma 混合检索结果已通过端到端组合测试。
- 36 项测试、Ruff 和 mypy 均通过，不需要 API Key 或模型下载。
- M4 达到完成条件，可以在获得批准后进入 Agentic 工作流阶段。

## M5：Agentic 工作流

### 目标

- 使用 LangGraph 建立面向本地硬件安全论文的显式状态机。
- 完成问题分类、检索规划、领域术语扩展和分轮检索。
- 结构化评价证据充分性，并根据证据缺口改写查询。
- 对引用执行完整性与语义支持两层验证。
- 引用验证失败时最多修复一次，所有循环达到上限后明确拒答。
- 输出不包含隐藏推理的执行轨迹，为 API 和 UI 提供数据基础。

### 完成结果

| 项目 | 结果 |
| --- | --- |
| 工作流底座 | LangGraph `StateGraph`，独立 `hardsec_scholar.agent` 模块 |
| 图节点 | 分类/规划、术语扩展、检索、评价、改写、生成、验证、修复、完成、拒答 |
| 问题类型 | fact、mechanism、threat_model、experiment、metric、comparison、limitation |
| 检索计划 | 子问题、优先章节、是否比较的结构化 `QuestionPlan` |
| 领域扩展 | 复用确定性硬件安全术语表，并限制每轮最多 6 个查询 |
| 证据评价 | 结构化充分性、选定 Evidence ID 和缺失证据维度 |
| 查询重试 | 默认最多 2 次；拒绝与历史查询重复的改写 |
| 引用验证 | 先校验 ID/元数据完整性，再由独立模型判断语义支持 |
| 答案修复 | 默认最多 1 次；修复后必须再次通过两层引用验证 |
| 防无限循环 | 检索、修复、结构化输出重试和 LangGraph recursion limit 均有上限 |
| 执行结果 | 返回计划、全部查询、改写原因、重试次数、修复次数和安全轨迹 |
| 轨迹事件 | question_classified、retrieval_planned、query_expanded、retrieval_started、evidence_retrieved、evidence_graded、query_rewritten、answer_generating、citation_verifying、completed |
| 测试 | 43 个测试全部通过，Agent 路由测试不调用真实 LLM 或 API |
| Ruff | 53 个项目文件格式和 Lint 检查通过 |
| mypy | 34 个源码文件全部通过 |

### 新增结构

```text
src/hardsec_scholar/agent/
├── __init__.py
├── models.py
├── interfaces.py
├── reasoner.py
└── workflow.py

tests/integration/
└── test_agentic_workflow.py
```

### 工作流路由

```text
classify_question
  -> expand_domain_terms
  -> retrieve_evidence
  -> grade_evidence
       | sufficient
       v
     generate_answer -> verify_citations -> complete
                            | failed and repair remains
                            v
                       repair_answer --+
       | insufficient                      |
       | retry remains                     |
       v                                   |
     rewrite_query -> retrieve_evidence    |
       | duplicate / limit reached         |
       +---------------------------> abstain
```

### 问题与解决措施

| 编号 | 问题 | 影响 | 解决措施 | 状态 |
| --- | --- | --- | --- | --- |
| M5-01 | Open Deep Research 上游图面向开放网络深度研究，节点和工具范围大于本地 10 篇论文场景 | 直接复用整张图会引入不需要的 Web 工具、Supervisor 和并发复杂度 | 复用其 LangGraph 技术底座，在独立包中实现本地论文专用的有界子图，不修改上游图 | 已解决 |
| M5-02 | 模型可能重复历史查询，导致无效循环 | 消耗模型调用且无法补足证据 | 对改写查询做大小写无关的规范化去重；重复改写立即停止并拒答 | 已解决 |
| M5-03 | 模型证据评价器可能选择未检索到或重复的 Evidence ID | 生成阶段可能接收伪造或重复来源 | 在进入生成节点前以本轮上下文白名单重新解析；非法选择降级为证据不足 | 已解决 |
| M5-04 | 只限制业务重试次数仍可能触发 LangGraph 默认递归上限，或配置放大后出现无限循环风险 | 高重试配置下运行不稳定 | 检索与修复次数分别校验，并根据两个上限计算显式 recursion limit | 已解决 |
| M5-05 | 引用 ID 正确但 Claim 可能夸大证据含义 | 输出“真引用、错结论” | 新增独立语义引用复核；失败后只允许使用原证据修复，重新验证仍失败则拒答 | 已解决 |
| M5-06 | 结构化模型偶发格式错误 | 单次解析失败会中止整条工作流 | 每个结构化推理适配器使用最多 3 次的 LangChain 有界重试 | 已解决 |
| M5-07 | 完整轨迹可能泄露隐藏推理或论文全文 | 日志与 UI 存在隐私风险 | 轨迹只保存节点、事件名、计数和简短结果，不保存 chain-of-thought、完整提示或正文 | 已解决 |
| M5-08 | 离线固定响应只能验证路由，不能代表真实模型决策质量 | 证据评价和语义验证效果尚未量化 | M5 保证状态机确定性与安全边界；真实模型质量在 M7 评估集和对照实验中测量 | 已记录 |
| M5-09 | PyMuPDF 的 5 条 SWIG 弃用警告继续出现在完整测试中 | 测试输出包含第三方警告 | 与 Agent 图无关且不影响结果，继续保留观察 | 已记录 |

### 方案动态调整

1. **底座复用粒度**：不把 Web-first 的上游 Supervisor 图直接塞入本地问答链路；保留 Open Deep Research 代码和依赖，同时用相同 LangGraph 模式构建垂直领域图，减少修改上游产生的升级冲突。
2. **规划与执行分离**：`QuestionPlan` 只负责问题类型、子问题和优先章节；查询文本还要经过确定性术语扩展，并受每轮最大查询数约束。
3. **证据白名单**：检索器、证据评价器、生成器三个边界都使用稳定 Evidence ID；任何模型都不能把上下文外 ID 提升为可引用证据。
4. **双层引用验证**：M4 的确定性完整性验证保留为第一层，M5 增加独立语义支持判断作为第二层。修复不会重新开放检索或引入新来源。
5. **可观测性前置**：原方案把执行轨迹主要放在 UI 阶段；本阶段先建立 `TraceEvent` 和 `AgentRun`，下一阶段只需做流式传输与展示，不必反向修改工作流。
6. **失败语义**：证据不足、重复改写、非法证据选择、语义验证失败都属于可解释拒答，不抛出伪答案；真正的运行异常留给 API 层统一处理。

### 阶段验收结论

- Agent 能分类问题、制定检索计划并对硬件安全术语生成受限查询变体。
- 初始证据不足时会根据具体缺口改写查询，最多重试配置次数。
- 重复查询改写会被拒绝，不会形成无效循环。
- 证据评价器不能选择检索上下文之外或重复的 Evidence ID。
- 生成答案必须先后通过引用完整性和语义支持验证。
- 语义验证失败时最多修复一次；修复仍失败则返回证据不足拒答。
- 运行结果包含可供面试演示的计划、查询、改写原因和节点轨迹，不含隐藏推理。
- 43 项测试、Ruff 和 mypy 均通过，测试无需 API Key。
- M5 达到完成条件，可以在获得批准后进入 API、UI 与执行轨迹展示阶段。

## M6：API、UI 与执行轨迹

### 目标

- 提供本地 FastAPI 服务，暴露论文、证据、对话、运行和轨迹接口。
- 支持 PDF 上传、解析、索引、元数据修改、重新索引和完整删除。
- 将 Agent 节点事件通过 SSE 在运行过程中实时传输。
- 持久化对话消息、完整运行结果和安全轨迹摘要。
- 建立可直接演示的论文库、智能问答、引用详情和 Agent 轨迹界面。
- 保持论文、数据库、索引和模型密钥只在本机。

### 完成结果

| 项目 | 结果 |
| --- | --- |
| API 框架 | FastAPI，本地默认地址 `127.0.0.1:8000` |
| 论文接口 | 上传、列表、详情、元数据修改、删除、重新索引 |
| 文件防护 | 仅接受 PDF、流式限制最大体积、临时文件始终清理 |
| 证据接口 | 按稳定 Evidence ID 返回论文、章节、页码和原文 |
| 对话接口 | 创建与读取持久化对话，保存用户和助手消息 |
| Agent 运行 | 同步 JSON 接口和实时 SSE 接口同时提供 |
| SSE | 节点完成即传输安全 TraceEvent，最终发送持久化 Run |
| 历史存储 | SQLite 保存 conversations、messages、runs、run_events |
| 数据库索引 | 对话消息和运行按 conversation + created_at 建立索引，并执行 `PRAGMA optimize` |
| 前端 | React 19 + Vinext + TypeScript，位于 `web/` |
| 研究界面 | 论文筛选、问题建议、实时阶段、答案、拒答、检索详情和查询改写原因 |
| 引用查看 | 点击行内 Evidence ID 打开来源抽屉，展示标题、章节、页码和原文 |
| 论文库 | 拖放上传、状态、方向、页数、Chunk 数、编辑、重索引和删除 |
| 响应式设计 | 桌面、平板和移动端布局，交互元素具备标签与键盘语义 |
| 本地运行 | 新增 `hardsec-scholar-api` 命令和中文运行指南 |
| CI | Python 质量任务之外新增 Node 24、npm ci、ESLint、构建和渲染测试 |
| 后端测试 | 47 项全部通过，其中 API 覆盖上传、编辑、删除、对话、SSE 和限制校验 |
| 前端测试 | ESLint 通过、生产构建通过、2 项服务端渲染测试通过 |
| mypy | 40 个源码文件全部通过 |

### 新增结构

```text
src/hardsec_scholar/api/
├── __init__.py
├── app.py
├── cli.py
├── history.py
├── models.py
└── services.py

web/
├── .openai/hosting.json
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   └── globals.css
├── tests/rendered-html.test.mjs
├── package.json
├── package-lock.json
└── vite.config.ts

docs/
└── LOCAL_APP.md
```

### API 路由

```text
GET    /api/health
POST   /api/papers
GET    /api/papers
GET    /api/papers/{paper_id}
PATCH  /api/papers/{paper_id}
DELETE /api/papers/{paper_id}
POST   /api/papers/{paper_id}/reindex
GET    /api/evidence/{evidence_id}

POST   /api/conversations
GET    /api/conversations/{conversation_id}
POST   /api/conversations/{conversation_id}/messages
POST   /api/conversations/{conversation_id}/messages/stream
GET    /api/runs/{run_id}
GET    /api/runs/{run_id}/events
```

### 问题与解决措施

| 编号 | 问题 | 影响 | 解决措施 | 状态 |
| --- | --- | --- | --- | --- |
| M6-01 | Sites 官方初始化脚本依赖 Bash，当前 Windows 环境没有可用 Bash；脚本还会在 `web/` 中初始化嵌套 Git | 无法直接运行脚本，且嵌套仓库会破坏当前项目结构 | 复用同一官方 Vinext 模板，用 PowerShell复制到 `web/`，保留唯一根仓库 | 已解决 |
| M6-02 | 首次 npm 安装受系统全局缓存权限影响并留下不完整依赖目录 | `npm ci` 失败，ESLint 误扫描隔离文件 | 将失败目录移动到被忽略的项目缓存，改用 `web/.npm-cache` 后安装成功 | 已解决 |
| M6-03 | 模板 npm 脚本使用 POSIX 环境变量写法，在 Windows cmd 中不可执行 | 无法启动开发服务器和构建 | 移除脚本中的行内环境赋值；Vite 配置已自行设置等价日志路径 | 已解决 |
| M6-04 | 原 Agent `run()` 只有结束后才返回完整轨迹 | UI 无法实时展示正在执行的节点 | 基于 LangGraph `stream_mode=values` 新增 `stream()`，逐节点发出新增 TraceEvent，最后返回 AgentRun |
| M6-05 | 用户与助手消息可能在同一时钟精度内获得相同时间戳 | SQLite 按时间和随机 ID 排序时消息顺序偶尔反转 | 同时间戳使用 SQLite `rowid` 插入顺序作为稳定次序 | 已解决 |
| M6-06 | API 启动时若立即创建模型、Embedding 和 Reranker，缺少密钥就会导致论文列表也不可用 | 离线论文管理与在线问答错误耦合 | 仓库和摄取服务立即创建，模型工作流延迟到首次索引或问答时创建 | 已解决 |
| M6-07 | 上传成功但 Embedding 配置缺失时无法完成向量索引 | 整个上传若回滚，用户需要重复解析 PDF | 保留已解析论文为 `parsed`，响应中返回明确 indexing warning，补全配置后可点击 Reindex | 已解决 |
| M6-08 | 删除论文时不能依赖在线 Embedding 凭据，否则可能残留向量 | 缺少密钥时无法满足无孤立索引要求 | 使用 Chroma 本地客户端直接按 paper_id 删除向量，再删除 SQLite 和受控 PDF | 已解决 |
| M6-09 | 上传体积只在 PDF 解析阶段校验会先把整个文件写到磁盘 | 超大请求可能占用过多本地空间 | 上传流每 1 MB 累计字节，超过配置立即返回 413，并在 finally 清理临时文件 | 已解决 |
| M6-10 | 前端遮罩最初使用带点击事件的非交互 div | 键盘用户无法关闭引用详情，ESLint 可访问性检查失败 | 改为具有 aria-label 的真实 button 遮罩，并保留明确关闭按钮 | 已解决 |
| M6-11 | FastAPI TestClient 产生一条迁移到 httpx2 的第三方弃用警告 | 测试输出新增 1 条警告 | 当前 47 项测试均通过；等待 FastAPI/Starlette 稳定迁移后统一升级，不在本阶段强制替换测试客户端 | 已记录 |
| M6-12 | Vinext 对动态客户端 API 页给出无法静态分类的提示 | 构建输出含非阻塞提示 | 页面生产构建和服务端渲染均成功；该提示属于 Vinext beta 静态分析限制 | 已记录 |
| M6-13 | Vinext 开发服务器在连续 HMR 更新后提示多个 renderer 共享 Context | 开发控制台出现警告，但页面仍持续返回 200 | 确认生产构建、SSR 测试和重新启动均正常；作为 Vinext beta 开发热更新限制记录，不影响交付构建 | 已记录 |

### 方案动态调整

1. **本地优先而非发布**：Sites 技能通常建议部署，但本项目开发文档明确论文、SQLite、Chroma 和密钥只保留在本机。本阶段只构建和预览，不创建 D1/R2，不公开部署，也不生成社交分享图。
2. **实时接口**：保留普通同步消息接口便于测试和脚本调用，同时增加 POST SSE 接口供网页实时消费 Agent 节点事件。
3. **运行持久化**：SSE 运行使用预生成 run_id；节点事件实时发送，终态再以同一 ID 原子保存消息、答案和全量轨迹，随后可通过 GET 重放。
4. **前后端边界**：React 页面不把论文、对话或运行历史存入 localStorage；SQLite 是唯一事实来源，浏览器状态只保存当前交互。
5. **模型配置失败方式**：论文 CRUD 和历史读取无需模型配置。索引或问答所需配置缺失时返回可操作的 503/警告，不让整个 API 无法启动。
6. **界面信息架构**：不建立多个空泛 Dashboard 页面；第一版集中为 Research 和 Paper library 两个核心工作区，并把引用与轨迹作为就地详情展示。
7. **UI 视觉方向**：采用科研笔记/实验记录风格，以深绿、纸张灰和荧光黄区分应用边界、正文与可验证证据，不依赖装饰性图片。

### 阶段验收结论

- 用户可以通过本地网页上传论文、查看解析/索引状态、修正元数据、重索引和删除。
- 用户可以选择全部或部分已索引论文并提交研究问题。
- Agent 运行过程通过 SSE 实时展示分类、规划、检索、评价、改写、生成和验证事件。
- 回答中的 Evidence ID 可展开查看论文标题、章节、页码和原文。
- 拒答、缺失模型配置、上传限制和 API 连接失败都有明确界面反馈。
- 对话、最终答案和安全轨迹保存在 SQLite，而非浏览器临时存储。
- 后端 47 项测试、Ruff、mypy、前端 ESLint、生产构建和 2 项渲染测试全部通过。
- API 与网页开发服务已分别在本机 8000 和 3000 端口完成响应检查。
- M6 达到完成条件，可以在获得批准后进入评估、对照实验和面试交付阶段。

## M7：评估、对照实验与交付材料

### 检查点 1：真实语料预检与导入

#### 当前结果

| 检查项 | 结果 |
| --- | --- |
| DeepSeek 连通性 | `deepseek-v4-flash` 普通调用成功 |
| Embedding 连通性 | SiliconFlow `BAAI/bge-m3` 成功返回 1024 维向量 |
| 真实论文 | 10 篇全部解析并索引 |
| 语料规模 | 157 页、324 个切片 |
| 索引状态 | 10/10 为 `indexed` |
| Agent 冒烟测试 | Revizor 问题成功完成规划、检索、生成和引用验证 |
| 引用检查 | 命中 Revizor 第 2–3 页的真实证据 |

#### 问题与解决措施

| 编号 | 问题 | 影响 | 解决措施 | 状态 |
| --- | --- | --- | --- | --- |
| M7-01 | Embedding 地址为 SiliconFlow，但模型名仍为 OpenAI `text-embedding-3-small` | 接口返回模型不存在，无法索引 | 将非敏感模型配置调整为 `BAAI/bge-m3`，密钥和地址保持不变 | 已解决 |
| M7-02 | DeepSeek V4 默认 Thinking 模式不兼容 LangChain 默认 `response_format`，且强制工具选择也会被拒绝 | Agent 在问题分类节点返回 HTTP 400 | DeepSeek 客户端显式关闭 Thinking；结构化输出统一使用 function calling，并新增回归断言 | 已解决 |
| M7-03 | Revizor PDF 含不规范 ICC profile | MuPDF 输出色彩配置警告 | 确认文本、页码、切片和索引均完整；作为非阻塞 PDF 质量问题保留记录 | 已记录 |
| M7-04 | 一篇论文标题包含 Unicode 连字 `ﬁ`，Windows GBK 控制台无法输出 | 首次批量导入日志中断 | 批处理显式使用 UTF-8；依靠内容哈希安全跳过已完成论文 | 已解决 |

#### 方案动态调整

1. **真实服务先行预检**：在建立人工评测集前，用两个最小调用分别验证 LLM 和 Embedding，避免把服务配置错误误判为检索质量问题。
2. **结构化输出兼容层**：保留 DeepSeek 作为生成模型，不更换供应商；仅在 DeepSeek 地址下关闭 Thinking，以稳定支持 Pydantic 结构化结果。
3. **渐进导入**：先导入两篇并完成真实问答，再扩展到全部十篇；内容哈希保证失败重试不会重复写入。

#### 检查点结论

- 真实论文、LLM、Embedding、混合检索和引用链已经连通。
- 当前只完成 M7 的语料预检与导入；30–50 题评测集、Baseline/Agentic 对照实验和面试交付材料仍未开始。
- 下一检查点应基于 10 篇论文建立可人工审查的评测集，在获得用户批准后执行。

### 检查点 2：真实论文评测集

#### 当前结果

| 检查项 | 结果 |
| --- | --- |
| 数据集 | `data/evaluations/hardsec_benchmark_v1.jsonl` |
| 题目总数 | 30 |
| 可回答 / 拒答 | 27 / 3 |
| 类别分布 | 单论文事实 10、机制 5、威胁模型 4、实验 4、比较 4、拒答 3 |
| 论文覆盖 | 10/10 |
| 金标准字段 | 参考答案、paper ID、chunk ID、预期页码、拒答标记 |
| 结构校验 | 30/30 通过 |
| 语义引用审核 | 27/27 可回答题通过 |
| 工程验证 | 50 项测试、Ruff、mypy 全部通过 |

#### 新增能力

- 独立的 `EvaluationSample` 与 `EvaluationCategory` 数据契约。
- JSONL 加载器和离线语料一致性校验器。
- 可复现的候选题生成、离线校验和在线语义审核命令。
- 评测集来源、类别、验证方式和局限性说明文档。

#### 问题与解决措施

| 编号 | 问题 | 影响 | 解决措施 | 状态 |
| --- | --- | --- | --- | --- |
| M7-05 | DeepSeek 首次为每个类别返回多个候选，而非恰好一题 | 类别数量超过批准配额 | 每类只选择首个结构有效候选，同时要求全部指定类别齐全 | 已解决 |
| M7-06 | 首次比较题只引用其中一篇论文 | 无法作为跨论文比较金标准 | 比较题强制每篇至少一个证据切片，并为单批生成增加最多 3 次重试 | 已解决 |
| M7-07 | 语义审核发现两道比较题含证据未直接说明的细节 | 参考答案存在轻微过度表述 | 删除 `gem5` 实现归属等细节，并将漏洞名称收紧为绑定摘要明确支持的描述；复审通过 | 已解决 |

#### 方案动态调整

1. **机器候选不等于金标准**：候选先经过 ID、论文归属和页码确定性校验，再逐题检查并使用独立引用节点语义复核。
2. **诚实标注数据集等级**：当前称为内部项目评测集，不宣称为同行评议或独立领域专家标注；公开发布结论前仍建议专家复核。
3. **固定 30 题第一版**：先采用最低完整规模控制 API 成本和人工检查量；后续可在相同模式下扩展到 50 题。

#### 检查点结论

- 已获得可用于相同输入条件下比较 Baseline RAG 与 Agentic RAG 的第一版真实评测集。
- 本检查点只建立数据和质量门槛，尚未运行两种系统的批量对照实验。
- 下一检查点应实现评估执行器与确定性指标，并运行首轮 Baseline/Agentic 对照实验。

### 检查点 3：Baseline / Agentic 对照实验

#### 实验配置

| 项目 | 配置 |
| --- | --- |
| 评测范围 | 30 题，两个系统均检索完整 10 篇语料，不传入金标准 paper ID |
| Baseline | Dense top-10 → 选择上下文 → 一次生成 → 引用完整性校验 |
| Agentic | 分类 → 扩展 → Hybrid/RRF/FlashRank → 评价 → 有界改写 → 生成 → 语义引用验证 |
| LLM | DeepSeek `deepseek-v4-flash`，非 Thinking function calling |
| Embedding | SiliconFlow `BAAI/bge-m3` |
| 结果策略 | JSONL 逐条追加、成功项断点续跑、失败项自动补跑、汇总采用每组最新记录 |

#### 最终结果

| 指标 | Dense Baseline | Agentic RAG |
| --- | ---: | ---: |
| Recall@5 | 0.6204 | 0.5494 |
| Recall@10 | 0.7346 | **0.7870** |
| MRR | **0.6584** | 0.5693 |
| 正确页命中率 | 0.9630 | **1.0000** |
| 全轮次金标准切片召回 | 0.7346 | **0.8519** |
| 回答/拒答正确率 | **0.9667** | 0.9000 |
| Reference token F1 | **0.4797** | 0.4616 |
| 引用页码精确率 | **0.7575** | 0.7041 |
| 平均延迟 | **2.3350 秒** | 8.2398 秒 |
| P95 延迟 | **3.8679 秒** | 12.0928 秒 |
| 最终 30 次 token | **128,510** | 324,954 |
| 最终 30 次估算 LLM 成本 | **$0.019459** | $0.048803 |

#### Agent 行为指标

- 平均查询数 4.1667。
- 20% 的问题触发至少一次检索改写，平均重试 0.4 次。
- 3.33% 的问题触发答案修复。
- 威胁模型类是 Agentic 最明确的优势类别：Recall@10 从 0.75 提升到 1.00，回答/拒答正确率从 0.75 提升到 1.00。

#### 问题与解决措施

| 编号 | 问题 | 影响 | 解决措施 | 状态 |
| --- | --- | --- | --- | --- |
| M7-08 | 原 `BasicRAGService` 可接任意检索器，若直接使用 Hybrid 就不是真正 Baseline | 对照定义不可信 | 新增 Dense-only top-10 适配器，两个系统共享同一向量库、生成模型和上下文限制 | 已解决 |
| M7-09 | DeepSeek 偶尔返回 HTTP 200 但无结构化工具参数 | 首轮 60 次中 6 项失败，原 `.with_retry()` 无法捕获校验层错误 | 将模型调用与 Pydantic 校验整体重试；连续 3 次空 QuestionPlan 时仅分类节点规则降级；失败项可续跑 | 已解决 |
| M7-10 | 证据评价器要求论文原文已经包含直接的并排比较 | 两篇独立证据齐全仍拒绝跨论文综合 | 明确允许从不同论文证据合成比较，并在未预选论文时要求至少两个相关 paper ID | 已解决 |
| M7-11 | 检索重试耗尽后，评价器虽选出有效证据仍直接拒答 | 后续生成和双重引用验证没有机会作最终判断 | 有选中证据时允许进入生成；生成拒答、引用完整性和语义验证继续 fail closed | 已解决 |
| M7-12 | Agentic 提高证据覆盖，但最终回答指标没有全面超过 Baseline | 不能诚实宣称 Agentic 普遍更优 | 停止针对剩余个别题定向调参，记录覆盖、质量、延迟和成本的真实权衡 | 已记录 |

#### 方案动态调整

1. **首轮结果驱动通用修复**：只修复可由接口行为和工作流逻辑解释的普遍问题；不依据金标准为单题编写规则。
2. **最新结果与总尝试分离**：公平对比使用最终 30 对 30；开发期失败和重跑成本单列。Agentic 总尝试成本约 `$0.067801`，不混入公平结果。
3. **不以 token F1 冒充语义正确率**：报告明确说明它只是透明的词汇重合指标；精确金标准引用也会惩罚有效的替代切片。
4. **负结果保留**：Agentic 的主要收益是更高 Recall@10、全轮次召回和页码命中；代价是当前版本更保守、约 3.5 倍延迟和 2.5 倍 token/费用。

#### 检查点结论

- 30 个 Baseline 和 30 个 Agentic 最终运行全部成功，评估执行器支持断点续跑与指定样本复跑。
- Agentic RAG 在证据覆盖和威胁模型问题上有优势，但当前整体答案与精确引用指标未超过 Dense Baseline。
- 评估报告已写入 `docs/EVALUATION_RESULTS.md`，可作为面试中解释工程权衡和迭代过程的核心材料。
- 下一检查点应整理 README、运行命令、演示脚本和面试讲解材料；M7 尚未整体完成。

### 检查点 4：交付材料与最终验收

#### 当前结果

| 交付项 | 结果 |
| --- | --- |
| 项目 README | 已改为 HardSec Scholar 中文项目主页，包含定位、架构、配置、运行、评测与限制 |
| 本地运行指南 | 已补齐 `.venv + pip + npm` 安装、DeepSeek/SiliconFlow 配置、手动运行和排障 |
| Windows 一键启停 | `scripts/start_local.ps1` 与 `scripts/stop_local.ps1` 已真实启停验证 |
| 面试材料 | 已完成 7 分钟演示流程、架构主线、常见追问和诚实表述清单 |
| 服务验证 | API `/api/health` 返回 `ok`，Web 返回 HTTP 200，停止后 3000/8000 端口均释放 |
| 后端质量门槛 | Ruff、mypy 通过，58 项测试通过；保留 6 条已知第三方警告 |
| 前端质量门槛 | ESLint、生产构建和 2 项渲染测试通过 |

#### 问题与解决措施

| 编号 | 问题 | 影响 | 解决措施 | 状态 |
| --- | --- | --- | --- | --- |
| M7-13 | 当前机器没有全局 `uv`，旧本地指南依赖 `python -m uv` | 新环境照文档无法直接安装或启动 | 交付文档统一改为标准 `.venv`、`pip install -e ".[dev]"` 和明确解释器路径 | 已解决 |
| M7-14 | 系统代理把首次 localhost 健康检查转发并返回 502 | 一键启动误判失败 | 使用禁用代理的独立 `HttpClientHandler` 检查本机服务 | 已解决 |
| M7-15 | 首次启动失败时只停止父进程，npm 的 Vinext 子进程仍监听 3000 | 回滚不完整，影响再次启动 | 启动失败和正常停止都递归枚举并终止已记录进程树；PID 启动时间防止误杀复用进程 | 已解决 |
| M7-16 | README 首版 Ruff 示例把整个上游 `tests/` 纳入检查 | Open Deep Research 的历史评估脚本触发 12 个非本项目 lint 问题 | 恢复既定质量边界：`conftest.py`、`unit`、`integration`；不修改不相关上游代码 | 已解决 |
| M7-17 | 根目录原始语料 `paper/` 未被 Git 忽略 | 公开仓库时可能误提交有分发限制的论文 PDF | 将 `paper/` 加入 `.gitignore`，并保留 `data/papers/` 受控副本规则 | 已解决 |

#### 方案动态调整

1. **不依赖额外包管理器**：一键启动只负责启动已经安装的环境，不在每次启动时隐式下载依赖或修改锁文件；首次安装步骤显式写在 README。
2. **可恢复的进程管理**：启动前检查端口和状态文件，成功后保存项目根目录、PID 与启动时间；停止时只处理匹配记录的进程树。
3. **隐私表述收紧**：文档将项目描述为“本地持久化、调用云模型”，明确提示检索证据会发送到配置的 LLM/Embedding 服务，避免把本地优先误称为完全离线。
4. **面试展示以实验为主线**：不只展示成功问答，同时展示拒答、引用回查和对照实验中的负结果，使项目价值落在可验证工程判断上。

#### 检查点与阶段结论

- README、运行指南、一键启停、面试讲解和评测报告已经形成完整交付链路。
- 一键启动、健康检查、网页响应和进程清理均在当前 Windows 环境实际验证。
- M7 的真实语料、评测集、对照实验和交付材料四个检查点全部完成。
- M0–M7 计划内开发已完成；后续工作属于演示彩排、公开仓库整理或基于评测结果的第二轮优化。

## M8：可控对话历史与历史管理

### 目标

- 允许用户按次决定是否保存对话历史。
- 提供历史列表、完整消息查看、继续会话和删除能力。
- 关闭保存时不创建空会话，也不持久化运行或轨迹。

### 当前结果

| 检查项 | 结果 |
| --- | --- |
| 保存开关 | Research 表单默认开启，可在运行前关闭 |
| 瞬时研究 | 新增 `POST /api/research/stream`，返回同样的 SSE 轨迹和答案但不写 SQLite |
| 历史列表 | `GET /api/conversations` 返回首个问题、更新时间和消息数 |
| 历史详情 | 复用 `GET /api/conversations/{id}` 展示有序的完整用户/助手消息 |
| 继续会话 | 恢复最近问题和运行结果，并将后续问答继续写入原会话 |
| 删除 | `DELETE /api/conversations/{id}` 级联删除消息、Agent run 和安全轨迹 |
| 自动化验证 | API 集成测试增至 6 项；前端 ESLint、生产构建和 2 项渲染测试通过 |
| 页面验证 | 本地浏览器验证开关、隐私提示、历史列表、详情和 Continue；真实用户历史未被删除 |

### 问题与解决措施

| 编号 | 问题 | 影响 | 解决措施 | 状态 |
| --- | --- | --- | --- | --- |
| M8-01 | 现有消息路由必须先创建 Conversation，并在结束时无条件保存 | 直接给请求加布尔值会在“关闭保存”时留下空会话 | 新增独立瞬时 SSE 路由，共享同一 Agent 工作流但只构造响应，不调用历史仓库 | 已解决 |
| M8-02 | React ESLint 禁止在 Effect 中同步读取偏好后立即 setState | 持久化开关偏好的实现未通过质量门槛 | 开关保持安全默认值“开启”，仅控制当前页面会话；不把偏好写入浏览器存储 | 已解决 |
| M8-03 | 原渲染测试依赖 `Agentic retrieval` 产品文案 | 增加开关时替换文案导致测试失败 | 保留原能力描述并追加存储状态说明，同时增加 History 和开关的渲染断言 | 已解决 |

### 方案动态调整

1. **瞬时运行独立路由**：避免用特殊 conversation ID 或事后删除模拟“不保存”，从数据模型上保证关闭保存不会留下记录。
2. **安全默认值**：每次页面加载默认保存历史；关闭只影响之后的运行，并切断当前持久化会话，重新开启时创建新会话。
3. **级联删除**：只删除会话记录，由 SQLite 外键级联处理消息、运行和轨迹，避免部分删除留下孤立数据。
4. **真实数据保护**：端到端页面验证只读取已有历史并测试 Continue；删除行为在临时数据库中验证，不操作用户真实历史。

### 阶段验收结论

- 用户可以明确控制某次研究是否进入本地历史。
- 关闭保存的 Agent 运行仍返回实时轨迹、答案和引用，但历史列表不增加记录。
- 历史可以完整查看、继续和永久删除；删除不存在的会话返回 404。
- M8 功能实现、自动化测试、本地页面验证和文档更新全部完成。

## 决策记录

| 编号 | 日期 | 决策 | 原因 |
| --- | --- | --- | --- |
| ADR-001 | 2026-08-20 | 使用 Open Deep Research `main` 作为底座 | 用户选定该项目，且其 LangGraph 工作流适合扩展 |
| ADR-002 | 2026-08-20 | HardSec Scholar 使用独立包扩展 | 降低与上游更新的冲突 |
| ADR-003 | 2026-08-20 | 当前保留 Python 3.10.8 | 上游实际最低要求为 Python 3.10 |
| ADR-004 | 2026-08-20 | 部署目标仍按 Python 3.11 设计 | 上游 `langgraph.json` 明确指定 3.11 |
| ADR-005 | 2026-08-20 | 新代码不依赖 `src/legacy` | legacy 已出现多项 LangGraph 弃用警告 |
| ADR-006 | 2026-08-20 | 使用 mypy 替代 Pyright | 复用上游开发依赖，降低工具链复杂度 |
| ADR-007 | 2026-08-20 | 发行名改为 `hardsec-scholar` | 明确项目身份，同时保留上游核心包兼容性 |
| ADR-008 | 2026-08-20 | API Key 延迟到客户端创建时校验 | 保证离线测试和静态检查无需密钥 |
| ADR-009 | 2026-08-20 | M2 仅建立 SQLite 语料索引 | 将解析正确性与检索算法解耦 |
| ADR-010 | 2026-08-20 | 使用稳定哈希生成论文 ID | 支持去重、可重复索引和稳定引用 |
| ADR-011 | 2026-08-20 | 规则解析结果允许后续人工修正 | 科研 PDF 版式差异无法由单一启发式完全覆盖 |
| ADR-012 | 2026-08-20 | 稠密索引使用本地 Chroma | 适合约 10 篇论文的个人项目，部署简单且可持久化 |
| ADR-013 | 2026-08-20 | 关键词检索使用 BM25Plus | 避免极小语料下 Okapi BM25 的零 IDF 问题 |
| ADR-014 | 2026-08-20 | 使用 RRF 融合两路排名 | 不要求两种检索分数处于同一尺度，逻辑透明且易解释 |
| ADR-015 | 2026-08-20 | Embedding 与 Reranker 采用协议边界 | 支持离线测试、运行时降级和后续模型替换 |
| ADR-016 | 2026-08-20 | 最终答案由结构化 Claim 确定性渲染 | 从结构上保证关键结论具有 Evidence ID |
| ADR-017 | 2026-08-20 | 引用校验失败时基础 RAG 直接拒答 | 防止伪造引用或无来源结论泄漏给用户 |
| ADR-018 | 2026-08-20 | M4 与 M5 拆分完整性和语义引用验证 | 基础能力保持确定性，语义复核与修复交由有界 Agent 循环 |
| ADR-019 | 2026-08-20 | 基于 LangGraph 新建本地论文专用 Agent 图 | 复用底座能力，同时避免 Web 深度研究图对个人项目过重 |
| ADR-020 | 2026-08-20 | 检索和答案修复使用相互独立的有界循环 | 防止一个失败路径无限放大模型调用 |
| ADR-021 | 2026-08-20 | 引用采用完整性校验加语义复核 | 同时防止伪造引用和引用不支持结论 |
| ADR-022 | 2026-08-20 | Agent 轨迹只记录安全摘要 | 支持 UI 可解释性，同时保护论文正文和模型隐藏推理 |
| ADR-023 | 2026-08-20 | API 和网页默认仅本地运行 | 保护未公开论文、索引和模型密钥 |
| ADR-024 | 2026-08-20 | SQLite 持久化对话、运行和轨迹 | 避免浏览器状态成为历史记录的事实来源 |
| ADR-025 | 2026-08-20 | 同时提供同步 JSON 和实时 SSE 消息接口 | 兼顾程序调用、测试和 UI 实时可观测性 |
| ADR-026 | 2026-08-20 | 在线模型客户端延迟初始化 | 让无密钥环境仍可管理本地论文和查看历史 |
| ADR-027 | 2026-08-20 | 第一版 UI 聚焦 Research 与 Paper library | 保持个人项目范围可控并突出面试演示主链路 |
| ADR-028 | 2026-08-21 | DeepSeek V4 结构化任务使用非 Thinking function calling | 当前 API 的 Thinking 模式拒绝 LangChain 强制工具选择 |
| ADR-029 | 2026-08-21 | M7 使用真实论文渐进导入后再建立评测集 | 将配置、解析问题与评测质量问题分开定位 |
| ADR-030 | 2026-08-21 | 第一版内部评测集固定为 30 题 | 满足开发文档下限，同时控制首轮审核与 API 成本 |
| ADR-031 | 2026-08-21 | 比较题金标准必须包含双方论文证据 | 防止只有问题形式是比较、实际答案却由单篇论文支撑 |
| ADR-032 | 2026-08-21 | 区分内部语义审核与独立专家标注 | 保持评估结论在面试和文档中的可信边界 |
| ADR-033 | 2026-08-21 | Baseline 固定为 Dense-only 单次检索与生成 | 避免用混合检索能力污染对照组定义 |
| ADR-034 | 2026-08-21 | 结构化响应重试必须覆盖 Pydantic 校验 | HTTP 成功不等于结构化结果有效 |
| ADR-035 | 2026-08-21 | 有效证据在重试耗尽后仍可进入双重验证生成 | 将最终安全判断交给生成拒答、引用完整性和语义验证，而非单一评价节点 |
| ADR-036 | 2026-08-21 | 不针对剩余失败题继续调参 | 防止测试集过拟合并保留可信的负结果 |
| ADR-037 | 2026-08-21 | Windows 交付使用显式 `.venv` 与记录式进程启停 | 降低环境依赖，并避免停止无关本机进程 |
| ADR-038 | 2026-08-21 | 对外表述为本地持久化而非完全离线 | 当前 DeepSeek 与 SiliconFlow 调用会把必要输入发送给云服务 |
| ADR-039 | 2026-08-21 | 不保存模式使用独立瞬时 SSE 路由 | 防止关闭保存时仍产生空会话或短暂落盘 |
| ADR-040 | 2026-08-21 | 会话删除依赖 SQLite 外键级联 | 原子清理消息、运行和轨迹，避免孤立历史数据 |
